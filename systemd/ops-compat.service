[Unit]
Description=OpenClaw Ops Compat Bridge (file→DB)
After=network.target

[Service]
Type=simple
WorkingDirectory=%h/workspace/repo
Environment=PYTHONPATH=%h/workspace/repo
EnvironmentFile=-%h/workspace/repo/.env
ExecStart=%h/workspace/repo/.venv/bin/python -m ops.compat
Restart=on-failure
RestartSec=10

[Install]
WantedBy=default.target
